docker/husarnet-config
# Ignore all .pyc files
# pyc files are compiled bytecode files that are generated by the Python interpreter 
# when a Python script is imported or executed. The . pyc files contain "compiled bytecode" 
# that can be executed directly by the interpreter, without the need to recompile the source 
# code every time the script is run
*.pyc

*.log
**/bin/

# Any nested build, install, or log directories in subdirectories are ignored
**/build/
**/install/
**/log/
